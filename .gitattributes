# Normalize line endings to LF for all text files (consistent across OSes).
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.svg text eol=lf
*.ico binary

# Shell scripts must stay LF (they run in Linux containers).
*.sh text eol=lf
